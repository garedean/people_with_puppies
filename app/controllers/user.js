import Ember from 'ember';

export default Ember.Controller.extend({
  // actions: {
  //   findMatches: function() {
  //     var currentUser = this.get('model');
  //     var allUsers = this.store.find('user');
  //     var userMatches = currentUser.get('matches');
  //     var twoWayMatches = [];
  //     allUsers.forEach(function(user){
  //         if (userMatches.includes(user) && user.get('matches').includes(currentUser)) {
  //           twoWayMatches.push(user)
  //         }
  //     });
  //     debugger;
  //       return twoWayMatches
  //   }
  // }
});
